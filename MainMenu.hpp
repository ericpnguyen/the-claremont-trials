#pragma once

#include "Menu.hpp"

class MainMenu : public Menu {
private:
  //  SDL_Img*;

public:
    MainMenu();
    ~MainMenu();

  //  virtual void click(SDL_Render*);
   // virtual void show(SDL_img*);
};